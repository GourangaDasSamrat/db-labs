import redisClient from "@/client";

const init = async () => {
  // streams add
  /*
  const xAdd = await redisClient.xadd(
    "temperatures:us-ny:10007",
    "*",
    "temp_f",
    "81.9",
    "pressure",
    "25.91",
    "humidity",
    "43",
  );
  console.log("xAdd result =>", xAdd);
*/
};

init();
